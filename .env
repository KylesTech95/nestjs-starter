LASTCHANGE="const [condition1,condition2,condition3] = [e.key===' ' && e.code==='Space',e.currentTarget,e.currentTarget]
        const {inputLocked} = FLAG;
        if(inputLocked)return;
        // console.log(e.key,e.code)
        if( condition1 ||
            condition2 ||
            condition3
        ){"